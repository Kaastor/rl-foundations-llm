"""Student-editable assignment surfaces.

Everything in this package is meant to be safe to modify without breaking
core infrastructure.

The core library (`course.core`) does **not** import this package.
CLI scripts pass assignment functions in explicitly.
"""
